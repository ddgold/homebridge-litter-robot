{
	"pluginAlias": "LitterRobot",
	"pluginType": "platform",
	"singular": true,
	"schema": {
		"type": "object",
		"properties": {
			"username": {
				"title": "Account Email",
				"description": "Your Whisker account email address.",
				"type": "string",
				"format": "email",
				"required": true
			},
			"password": {
				"title": "Account Password",
				"description": "Your Whisker account password.",
				"type": "string",
				"required": true
			},
			"showMotionSensor": {
				"title": "Show Motion Sensor",
				"description": "Expose the cleaning cycle as a Motion Sensor in HomeKit. Defaults to true.",
				"type": "boolean",
				"default": true
			},
			"showNightLight": {
				"title": "Night Light",
				"description": "Controls the night light in HomeKit. 'true' exposes it and sets On mode, 'auto' exposes it and sets Auto mode, 'false' hides it. Defaults to true.",
				"type": "string",
				"default": "true",
				"oneOf": [
					{ "title": "true", "enum": ["true"] },
					{ "title": "auto", "enum": ["auto"] },
					{ "title": "false", "enum": ["false"] }
				]
			},
			"showWasteDrawer": {
				"title": "Show Waste Drawer",
				"description": "Expose the waste drawer as a Filter Maintenance accessory in HomeKit. Defaults to true.",
				"type": "boolean",
				"default": true
			},
			"pollRate": {
				"title": "Poll Rate",
				"description": "How often (in seconds) to sync devices from the Whisker API. Defaults to 300 (5 minutes).",
				"type": "integer",
				"minimum": 60,
				"default": 300
			}
		}
	},
	"layout": [
		{
			"type": "tabs",
			"tabs": [
				{
					"title": "Account",
					"items": ["username", { "key": "password", "type": "password" }]
				},
				{
					"title": "Features",
					"items": ["showMotionSensor", "showNightLight", "showWasteDrawer", "pollRate"]
				}
			]
		}
	]
}
